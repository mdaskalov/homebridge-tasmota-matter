{
  "displayName": "Homebridge Tasmota Matter",
  "name": "homebridge-tasmota-matter",
  "version": "0.0.16",
  "description": "Control Tasmota Devices Using Matter",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/mdaskalov/homebridge-tasmota-matter.git"
  },
  "bugs": {
    "url": "https://github.com/mdaskalov/homebridge-tasmota-matter/issues"
  },
  "engines": {
    "node": "^22 || ^24",
    "homebridge": "^1.6.0 || ^2.0.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/**.ts --max-warnings=0",
    "watch": "npm run build && npm link && nodemon",
    "generateSchema": "ts-json-schema-generator --path 'src/schemas/tasmotaDevice.ts' --type 'TasmotaDeviceSchema' --out 'src/schemas/tasmota-device.json' --no-top-ref",
    "build": "rimraf ./dist && tsc",
    "prepublishOnly": "npm run lint && npm run build",
    "test": "npm run build && node dist/index.js && echo \"No syntax errors! (node $(node -v))\""
  },
  "keywords": [
    "homebridge-plugin",
    "tasmota",
    "matter"
  ],
  "dependencies": {
    "ajv": "^8.20.0",
    "mqtt": "^5.15.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "homebridge": "^2.3.0",
    "homebridge-config-ui-x": "^5.27.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.9.6",
    "rimraf": "^6.0.1",
    "ts-json-schema-generator": "^2.9.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0"
  }
}
